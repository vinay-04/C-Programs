// Print Characters: assign character in a program print it.

#include <stdio.h>
int main(){
    char ch; 
    printf("Enter a character");
    scanf("%c",&ch); // user input.
    printf("\n The ascii value is : %d", ch);
    return 0;
}